import Header from "../Header/Header";
import { Box } from "@mui/system";

const Graphs = () => {
  return (
    // HEADER
    <Box m="20px">
      <Box display="flex" justifyContent="space-between" alignItems="center">
        <Header title="GRAPHS" subtitle="Look at all them graphs!!" />
      </Box>
    </Box>
  );
};

export default Graphs;
